// ============================================================
// Constants
const BASE64_REGEXP = /^[a-zA-Z0-9-_]*$/;

/**
 * @name ResourceType
 * @enum
 */
const RESOURCE_TYPE = {
    ACCOUNT : 'account',
    IDENTITY: 'identity',
    TEXT: 'text',
};

// ============================================================
// Exports
export {
    BASE64_REGEXP,
    RESOURCE_TYPE,
};
